# Third-Party Notices

Notavia 使用 Go Modules、npm 包和容器镜像中的第三方开源软件。各组件仍受其原许可证约束；锁定版本见 `go.sum`、`pnpm-lock.yaml` 和 `docker-compose.yml`。

发布前 CI 会生成软件物料清单（SBOM）并执行许可证与漏洞扫描。导入到 Notavia 的网页、图片、音视频和文章版权仍归原权利人所有，不属于 Notavia 软件许可证授权范围。
